// best practice to use the base package as a module name (for public libraries)
// if we end the module name with a number we will get a warning to avoid version info
module com.example.greeter {
    
}
